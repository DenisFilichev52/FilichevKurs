<Application x:Class="FilichevKurs.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:FilichevKurs"
             StartupUri="View/Windowss/MainWin.xaml">
    <Application.Resources>
        <!-- Цветовая схема в военном стиле -->
        <SolidColorBrush x:Key="PrimaryColor" Color="#2F4F2F"/>
        <!-- Темно-зеленый -->
        <SolidColorBrush x:Key="SecondaryColor" Color="#556B2F"/>
        <!-- Оливковый -->
        <SolidColorBrush x:Key="AccentColor" Color="#8B4513"/>
        <!-- Коричневый (ржавчина) -->
        <SolidColorBrush x:Key="BackgroundColor" Color="#F5F5DC"/>
        <!-- Бежевый -->
        <SolidColorBrush x:Key="TextColor" Color="#2F2F2F"/>
        <!-- Темно-серый -->

        <!-- Стиль кнопок -->
        <Style x:Key="MilitaryButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryColor}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="BorderBrush" Value="{StaticResource SecondaryColor}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource SecondaryColor}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Стиль текстовых полей -->
        <Style x:Key="MilitaryTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource PrimaryColor}"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>

        <!-- Стиль PasswordBox -->
        <Style x:Key="MilitaryPasswordBox" TargetType="PasswordBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource PrimaryColor}"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>

        <!-- Стиль заголовков -->
        <Style x:Key="HeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="{StaticResource PrimaryColor}"/>
            <Setter Property="Margin" Value="0,0,0,20"/>
        </Style>
    </Application.Resources>
</Application>
